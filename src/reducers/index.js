const items = (state=[], action) => {
  switch(action.type) {
    case 'CREATE_ITEM':
    case 'FINISH_ITEM':
    case 'DELETE_ITEM':
      
    default:
      return state
  }
}

export default items